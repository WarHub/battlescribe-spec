<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>BattleScribeSpec.NewRecruit</RootNamespace>
    <Description>New Recruit roster engine adapter using Playwright browser automation.</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="BattleScribeSpec.Tests" />
    <!-- The UI roster driver creates NR lists too, so it must share this project's one correct
         list-deletion JS (NrListStoreJs) rather than keep its own copy. -->
    <InternalsVisibleTo Include="BattleScribeSpec.NrRosterUiDriver" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Playwright" GeneratePathProperty="true" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\BattleScribeSpec.TestKit\BattleScribeSpec.TestKit.csproj" />
    <ProjectReference Include="..\BattleScribeSpec.XmlGen\BattleScribeSpec.XmlGen.csproj" />
    <ProjectReference Include="..\BattleScribeSpec.Telemetry\BattleScribeSpec.Telemetry.csproj" />
    <ProjectReference Include="..\..\.deps\wham\src\WarHub.ArmouryModel.Source\WarHub.ArmouryModel.Source.csproj" />
    <ProjectReference Include="..\..\.deps\wham\src\WarHub.ArmouryModel.Source.BattleScribe\WarHub.ArmouryModel.Source.BattleScribe.csproj" />
  </ItemGroup>

</Project>
