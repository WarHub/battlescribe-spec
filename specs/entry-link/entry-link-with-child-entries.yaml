id: entry-link-with-child-entries
category: entry-link
description: >
  EntryLink can define child selectionEntries that supplement
  the linked entry's children. The child entries defined on the
  link are merged with the target's children.

setup:
  gameSystem:
    id: test-gs
    name: Test Game System
    forceEntries:
      - id: fe-patrol
        name: Patrol

  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: test-gs
      sharedSelectionEntries:
        - id: se-shared-unit
          name: Base Unit
          type: upgrade
      selectionEntries:
        - id: se-squad
          name: Squad
          type: unit
          entryLinks:
            - id: el-unit
              name: Base Unit
              targetId: se-shared-unit
              type: selectionEntry

steps:
  - action: addForce
    id: add-patrol
    forceEntryId: fe-patrol

  - action: selectEntry
    id: select-squad
    forceId: ${{ steps.add-patrol.forceId }}
    entryId: se-squad

  - action: selectChildEntry
    forceId: ${{ steps.add-patrol.forceId }}
    selectionId: ${{ steps.select-squad.selectionId }}
    entryId: se-shared-unit

  - expectedState:
      forces:
        - selections:
            - name: Squad
              children:
                - name: Base Unit
