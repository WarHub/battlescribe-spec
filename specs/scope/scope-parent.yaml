id: scope-parent
category: scope
engines:
  newrecruit: fail
description: Condition with scope=parent evaluates on the parent element

setup:
  gameSystem:
    id: test-gs
    name: Test Game System
    forceEntries:
      - id: fe-patrol
        name: Patrol
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: test-gs
      selectionEntries:
        - id: se-unit-a
          name: Unit A
          type: unit
          constraints:
            - id: con-max-3
              type: max
              value: 3
              field: selections
              scope: parent

steps:
  - action: addForce
    forceEntryIndex: 0

  - action: selectEntry
    forceIndex: 0
    entryIndex: 0

  - action: selectEntry
    forceIndex: 0
    entryIndex: 0

  - action: selectEntry
    forceIndex: 0
    entryIndex: 0

  # 3 selections at max scope
  - expectedState:
      forces:
        - selectionCount: 3
  - action: selectEntry
    forceIndex: 0
    entryIndex: 0

  # 4 selections - engine allows it
  - expectedState:
      forces:
        - selectionCount: 4
