id: gamesystem-root-selectionentry
category: gamesystem
description: >
  SelectionEntry defined at GameSystem level is available in all forces,
  regardless of which catalogue is used.

setup:
  gameSystem:
    id: test-gs
    name: Test Game System
    forceEntries:
      - id: fe-patrol
        name: Patrol
    selectionEntries:
      - id: se-gs-unit
        name: GS Unit
        type: unit

  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: test-gs

steps:
  - action: addForce
    id: add-patrol
    forceEntryId: fe-patrol

  - action: selectEntry
    forceId: ${{ steps.add-patrol.forceId }}
    entryId: UNKNOWN_ENTRY_0

  - expectedState:
      forces:
        - selectionCount: 1
          selections:
            - name: GS Unit
