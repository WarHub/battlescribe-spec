id: publication-create-and-fields
category: publication
description: >
  Add a publication to a catalogue and set its metadata fields (shortName, publisher,
  publicationDate). Verifies publication creation and its field surface.

setup:
  edit: cat-1
  gameSystem:
    id: gs-1
    name: Test System
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: gs-1

steps:
  - action: addEntry
    id: add-pub
    parentId: cat-1
    entryType: publication
    name: Codex

  - action: setFields
    entryId: ${{ steps.add-pub.entryId }}
    fields:
      shortName: Cdx
      publisher: Games Workshop
      publicationDate: "2023"
      publisherUrl: https://example.com

  - expectedState:
      catalogues:
        - id: cat-1
          publications:
            - name: Codex
              fields:
                shortName: Cdx
                publisher: Games Workshop
                publicationDate: "2023"
                publisherUrl: https://example.com
