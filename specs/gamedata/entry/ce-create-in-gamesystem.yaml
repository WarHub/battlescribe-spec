id: ce-create-in-gamesystem
category: entry
description: >
  Add a category entry to a game system and verify it appears in the
  categoryEntries container.
engines:
  nr-editor: skip # NewRecruit's data model does not support authoring category entries
  nr-editor-ui: skip # NewRecruit's data model does not support authoring category entries

setup:
  gameSystem:
    id: gs-1
    name: Test System

steps:
  - action: addEntry
    id: add-ce
    parentId: gs-1
    entryType: categoryEntry
    name: Troops

  - expectedState:
      gameSystem:
        id: gs-1
        categoryEntries:
          - name: Troops
            entryType: categoryEntry
