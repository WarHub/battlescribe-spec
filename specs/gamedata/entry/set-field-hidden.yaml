id: set-field-hidden
category: entry
description: >
  Set the hidden field of an entry and verify the state reflects it.

setup:
  edit: cat-1
  gameSystem:
    id: gs-1
    name: Test System
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: gs-1
      selectionEntries:
        - id: se-unit
          name: Stealth Unit
          type: unit

steps:
  - action: setFields
    entryId: se-unit
    fields:
      hidden: "true"

  - expectedState:
      catalogues:
        - id: cat-1
          selectionEntries:
            - id: se-unit
              name: Stealth Unit
              hidden: true
