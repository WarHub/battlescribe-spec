id: seg-set-field-hidden
category: entry
description: >
  Set the hidden field of a selection entry group and verify the state
  reflects it.

setup:
  gameSystem:
    id: gs-1
    name: Test System
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: gs-1
      sharedSelectionEntryGroups:
        - id: seg-1
          name: Hidden Options

steps:
  - action: setField
    entryId: seg-1
    field: hidden
    value: "true"

  - expectedState:
      catalogues:
        - id: cat-1
          sharedSelectionEntryGroups:
            - id: seg-1
              name: Hidden Options
              hidden: true
