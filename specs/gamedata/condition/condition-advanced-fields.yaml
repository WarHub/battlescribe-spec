id: condition-advanced-fields
category: condition
description: >
  Set the advanced query fields on a condition: childId, shared, percentValue and
  includeChildSelections. Verifies the condition query-field surface beyond
  type/value/field/scope.

engines:
  newrecruit: skip
  newrecruit-ui: skip

setup:
  gameSystem:
    id: gs-1
    name: Test System
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: gs-1
      categoryEntries:
        - id: ce-troops
          name: Troops
      selectionEntries:
        - id: se-1
          name: Tactical Squad
          type: unit

steps:
  - action: addEntry
    id: add-mod
    parentId: se-1
    entryType: modifier

  - action: setFields
    entryId: ${{ steps.add-mod.entryId }}
    fields:
      field: hidden

  - action: addEntry
    id: add-con
    parentId: ${{ steps.add-mod.entryId }}
    entryType: condition

  - action: setFields
    entryId: ${{ steps.add-con.entryId }}
    fields:
      scope: roster
      childId: ce-troops
      shared: "true"
      percentValue: "true"
      includeChildSelections: "true"

  - expectedState:
      catalogues:
        - id: cat-1
          selectionEntries:
            - id: se-1
              children:
                - entryType: modifier
                  children:
                    - entryType: condition
                      fields:
                        scope: roster
                        childId: ce-troops
                        shared: "true"
                        percentValue: "true"
                        includeChildSelections: "true"
