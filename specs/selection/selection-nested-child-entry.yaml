id: selection-nested-child-entry
category: selection
description: selectChildEntry can target deeply nested selections via selectionPath
tags: [selection, nested]

setup:
  gameSystem:
    id: test-gs
    name: Test GS
    forceEntries:
      - id: fe-army
        name: Army
  catalogues:
    - id: cat-1
      name: Cat
      gameSystemId: test-gs
      selectionEntries:
        - id: se-squad
          name: Squad
          type: unit
          selectionEntries:
            - id: se-weapon
              name: Weapon
              type: upgrade
              selectionEntries:
                - id: se-scope
                  name: Scope
                  type: upgrade

steps:
  - action: addForce
    id: add-army
    forceEntryId: fe-army

  - action: selectEntry
    id: select-squad
    forceId: ${{ steps.add-army.forceId }}
    entryId: se-squad

  # Add "Weapon" as child of "Squad" (selection 0)
  - action: selectChildEntry
    id: select-weapon
    forceId: ${{ steps.add-army.forceId }}
    selectionId: ${{ steps.select-squad.selectionId }}
    entryId: se-weapon

    forceId: ${{ steps.add-army.forceId }}
    selectionId: ${{ steps.select-squad.selectionId }}
    entryId: se-weapon

  - expectedState:
      forces:
        - name: Army

  # Add "Scope" as child of "Weapon" using selectionPath to target the nested selection
  - action: selectChildEntry
    forceId: ${{ steps.add-army.forceId }}
    selectionId: ${{ steps.select-weapon.selectionId }}
    entryId: se-scope
  # Add "Scope" as child of "Weapon" using selectionPath to target the nested selection
  - action: selectChildEntry
    forceId: ${{ steps.add-army.forceId }}
    selectionId: ${{ steps.select-weapon.selectionId }}
    entryId: se-scope

  - expectedState:
      forces:
        - name: Army
          selections:
            - name: Squad
              children:
                - name: Weapon
                  children:
                    - name: Scope
