id: selection-model-type
category: selection
description: Selection type is preserved as defined in the entry (model)

setup:
  gameSystem:
    id: test-gs
    name: Test Game System
    forceEntries:
      - id: fe-patrol
        name: Patrol
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: test-gs
      selectionEntries:
        - id: se-model-a
          name: Model A
          type: model

steps:
  - action: addForce
    id: add-patrol
    forceEntryId: fe-patrol

  - action: selectEntry
    forceId: ${{ steps.add-patrol.forceId }}
    entryId: se-model-a

  - expectedState:
      forces:
        - selections:
            - name: Model A
              type: model
