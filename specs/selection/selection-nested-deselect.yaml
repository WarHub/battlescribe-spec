id: selection-nested-deselect
category: selection
description: deselectSelection can target nested selections via selectionPath
tags: [selection, nested]

setup:
  gameSystem:
    id: test-gs
    name: Test GS
    forceEntries:
      - id: fe-army
        name: Army
  catalogues:
    - id: cat-1
      name: Cat
      gameSystemId: test-gs
      selectionEntries:
        - id: se-squad
          name: Squad
          type: unit
          selectionEntries:
            - id: se-weapon
              name: Weapon
              type: upgrade

steps:
  - action: addForce
    id: add-army
    forceEntryId: fe-army

  - action: selectEntry
    id: select-squad
    forceId: ${{ steps.add-army.forceId }}
    entryId: se-squad

  - action: selectChildEntry
    id: select-weapon
    forceId: ${{ steps.add-army.forceId }}
    selectionId: ${{ steps.select-squad.selectionId }}
    entryId: se-weapon

  - expectedState:
      forces:
        - name: Army
          selections:
            - name: Squad
              children:
                - name: Weapon

  # Deselect the nested Weapon
  - action: deselectSelection
    forceId: ${{ steps.add-army.forceId }}
    selectionId: ${{ steps.select-weapon.selectionId }}

  - expectedState:
      forces:
        - name: Army
          selections:
            - name: Squad
              childCount: 0
