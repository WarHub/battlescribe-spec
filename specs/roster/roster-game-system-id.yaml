id: roster-game-system-id
category: roster
description: Roster correctly preserves game system ID from setup

setup:
  gameSystem:
    id: my-custom-gs-id
    name: Custom System
    forceEntries:
      - id: fe-patrol
        name: Patrol
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: my-custom-gs-id

steps:
  - expectedState:
      forceCount: 0
