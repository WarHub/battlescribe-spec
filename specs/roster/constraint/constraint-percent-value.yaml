id: constraint-percent-value
category: constraint
description: >
  Constraint with percentValue=true calculates threshold as a percentage
  of the total count in the given scope. For max=50 and field=selections,
  the threshold is 50% of total parent selections.
  Both engines handle this correctly.

setup:
  gameSystem:
    id: test-gs
    name: Test GS
    categoryEntries:
      - id: cat-troops
        name: Troops
    costTypes:
      - id: pts
        name: pts
        defaultCostLimit: 2000
    forceEntries:
      - id: fe-1
        name: Detachment
        categoryLinks:
          - id: cl-fe-troops
            targetId: cat-troops
            name: Troops
  catalogues:
    - id: cat-1
      name: Cat
      gameSystemId: test-gs
      selectionEntries:
        - id: se-1
          name: Elite
          type: unit
          categoryLinks:
            - id: cl-se1-troops
              targetId: cat-troops
              name: Troops
              primary: true
          costs:
            - name: pts
              typeId: pts
              value: 300
          constraints:
            - id: c1
              type: max
              value: 50
              field: selections
              scope: parent
              percentValue: true

steps:
  - action: addForce
    id: add-detachment
    forceEntryId: fe-1

  - expectedState:
      forces:
        - selectionCount: 0

  - action: selectEntry
    forceId: ${{ steps.add-detachment.forceId }}
    entryId: se-1

  # percentValue=true: max = 50% of parent selections.
  # With 1 total selection: threshold = 1/100*50 = 0.5.
  # 1 selection > 0.5 → both engines produce an error.
  - expectedState:
      errors:
        - on: selection se-1
          from: se-1/c1
      forces:
        - selectionCount: 1
          selections:
            - name: Elite
