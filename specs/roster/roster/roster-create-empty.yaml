id: roster-create-empty
category: roster
description: Creating a new roster results in an empty roster with no forces

setup:
  gameSystem:
    forceEntries:
      - id: fe-patrol
        name: Patrol
  catalogues:
    - id: cat-1

steps:
  - expectedState:
      forceCount: 0
