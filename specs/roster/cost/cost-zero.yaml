id: cost-zero
category: cost
description: Selection with zero cost retains a 0-valued cost entry

setup:
  gameSystem:
    costTypes:
      - id: pts
        name: Points
    forceEntries:
      - id: fe-patrol
        name: Patrol
  catalogues:
    - id: cat-1
      selectionEntries:
        - id: se-unit-a
          name: Free Unit
          type: unit
          costs:
            - name: Points
              typeId: pts
              value: 0

steps:
  - action: addForce
    id: add-patrol
    forceEntryId: fe-patrol

  - action: selectEntry
    forceId: ${{ steps.add-patrol.forceId }}
    entryId: se-unit-a

  - expectedState:
      forces:
        - selections:
            - name: Free Unit
              costs:
                - typeId: pts
                  value: 0
