id: auto-select-nested-child
category: auto-select
description: Auto-selected parent entry also triggers auto-selection of its children with min constraints
tags: [auto-select, constraint, min, nested]

setup:
  gameSystem:
    forceEntries:
      - id: fe-1
        name: Detachment
  catalogues:
    - id: cat-1
      selectionEntries:
        - id: se-parent
          name: Parent Unit
          type: unit
          constraints:
            - id: con-parent-min
              type: min
              value: 1
              field: selections
              scope: parent
          selectionEntries:
            - id: se-child
              name: Mandatory Upgrade
              type: upgrade
              constraints:
                - id: con-child-min
                  type: min
                  value: 1
                  field: selections
                  scope: parent

steps:
  - action: addForce
    forceEntryId: fe-1

  # Parent Unit is auto-selected (min=1). Its child Mandatory Upgrade
  # is also auto-selected because it has min=1.
  - expectedState:
      forces:
        - selections:
            - name: Parent Unit
              type: unit
              children:
                - name: Mandatory Upgrade
                  type: upgrade
                  number: 1
