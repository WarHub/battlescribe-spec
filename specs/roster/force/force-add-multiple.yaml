id: force-add-multiple
category: force
description: Multiple forces can be added from the same force entry

setup:
  gameSystem:
    forceEntries:
      - id: fe-patrol
        name: Patrol
  catalogues:
    - id: cat-1

steps:
  - action: addForce
    forceEntryId: fe-patrol

  - action: addForce
    forceEntryId: fe-patrol

  - expectedState:
      forces:
        - name: Patrol
        - name: Patrol
