id: force-add-single
category: force
description: Adding a force creates one force in the roster

setup:
  gameSystem:
    id: test-gs
    name: Test Game System
    forceEntries:
      - id: fe-patrol
        name: Patrol
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: test-gs

steps:
  - action: addForce
    forceEntryId: fe-patrol

  - expectedState:
      forces:
        - name: Patrol
