id: roster-create-empty
category: roster
description: Creating a new roster results in an empty roster with no forces

setup:
  gameSystem:
    id: test-gs
    name: Test Game System
    forceEntries:
      - id: fe-patrol
        name: Patrol
  catalogues:
    - id: cat-1
      name: Test Catalogue
      gameSystemId: test-gs

steps:
  - expectedState:
      forceCount: 0
