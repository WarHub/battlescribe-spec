id: gamesystem-root-rule
category: gamesystem
description: >
  Rule defined at GameSystem level appears on selections that reference it
  via infoLinks.

setup:
  gameSystem:
    forceEntries:
      - id: fe-patrol
        name: Patrol
    sharedRules:
      - id: rule-gs
        name: Core Rule
        description: A game-wide rule

  catalogues:
    - id: cat-1
      selectionEntries:
        - id: se-unit
          name: Unit A
          type: unit
          infoLinks:
            - id: il-rule
              name: Core Rule
              targetId: rule-gs
              type: rule

steps:
  - action: addForce
    id: add-patrol
    forceEntryId: fe-patrol

  - action: selectEntry
    forceId: ${{ steps.add-patrol.forceId }}
    entryId: se-unit

  - expectedState:
      forces:
        - selections:
            - name: Unit A
              rules:
                - name: Core Rule
                  description: A game-wide rule
