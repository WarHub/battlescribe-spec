id: selection-set-child-count-collective
category: selection
description: >
  setSelectionCount on a collective child entry. Verifies number=N on a single
  selection with scaled costs (N × per-unit cost).
tags:
  - selection
  - number
  - cost

setup:
  gameSystem:
    costTypes:
      - id: pts
        name: pts
    forceEntries:
      - id: fe-1
        name: Detachment
  catalogues:
    - id: cat-1
      selectionEntries:
        - id: se-squad
          name: Squad
          type: unit
          selectionEntries:
            - id: se-weapon
              name: Weapon
              type: upgrade
              collective: true
              costs:
                - name: pts
                  typeId: pts
                  value: 5

steps:
  - action: addForce
    id: add-detachment
    forceEntryId: fe-1

  - action: selectEntry
    id: select-squad
    forceId: ${{ steps.add-detachment.forceId }}
    entryId: se-squad

  - action: selectChildEntry
    id: select-weapon
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-squad.selectionId }}
    entryId: se-weapon

  - expectedState:
      costs:
        - typeId: pts
          value: 5
      forces:
        - selections:
            - name: Squad
              children:
                - name: Weapon
                  number: 1
                  costs:
                    - name: pts
                      typeId: pts
                      value: 5

  # setSelectionCount to 3 on the collective child
  - action: setSelectionCount
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-weapon.selectionId }}
    count: 3

  - expectedState:
      costs:
        - typeId: pts
          value: 15
      forces:
        - selections:
            - name: Squad
              children:
                - name: Weapon
                  number: 3
                  costs:
                    - name: pts
                      typeId: pts
                      value: 15
