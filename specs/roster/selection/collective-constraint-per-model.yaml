id: collective-constraint-per-model
category: selection
description: >
  Constraint validation on a collective entry is evaluated per-model:
  the actual count is divided by the parent's number before comparing
  against the constraint limit.
tags:
  - collective
  - constraint
  - validation

engines:
  # Real desktop BS enforces a collective child's "max" constraint as a hard
  # bound on the per-model count Spinner in the parent's edit panel: verified
  # live (bs-ui agent) that once Weapon is at its max (2 per model), clicking
  # the increment arrow, calling the spinner's own setSpinnerValue(3) or even
  # setSpinnerValue(100) directly, and Ctrl+D duplicating the selection all
  # leave it clamped at 2 — the JavaFX IntegerSpinnerValueFactory's max is
  # wired from the constraint, so no UI gesture (click, direct value commit,
  # or duplicate) can ever reach the per-model 3 this spec's final action
  # requires. A real desktop-BS user cannot perform this spec's last
  # setSelectionCount; the in-process reference/newrecruit engines still
  # cover it. See issue #309, PR #310.
  battlescribe-ui: skip

setup:
  gameSystem:
    costTypes:
      - id: pts
        name: pts
    forceEntries:
      - id: fe-1
        name: Detachment
  catalogues:
    - id: cat-1
      selectionEntries:
        - id: se-squad
          name: Squad
          selectionEntries:
            - id: se-trooper
              name: Trooper
              selectionEntries:
                - id: se-weapon
                  name: Weapon
                  collective: true
                  constraints:
                    - id: con-max-2
                      type: max
                      value: 2
                      field: selections
                      scope: parent
                  costs:
                    - name: pts
                      typeId: pts
                      value: 5

steps:
  - action: addForce
    id: add-detachment
    forceEntryId: fe-1

  - action: selectEntry
    id: select-squad
    forceId: ${{ steps.add-detachment.forceId }}
    entryId: se-squad

  - action: selectChildEntry
    id: select-trooper
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-squad.selectionId }}
    entryId: se-trooper

  - action: selectChildEntry
    id: select-weapon
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-trooper.selectionId }}
    entryId: se-weapon

  # Set Trooper to 3 — Weapon propagates to number=3 (1 per model)
  - action: setSelectionCount
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-trooper.selectionId }}
    count: 3

  # Weapon has max=2 per model. Currently at 1 per model (3 total / 3 models).
  # setSelectionCount to 2 per model → actual=6. No error.
  - action: setSelectionCount
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-weapon.selectionId }}
    count: 2

  - expectedState:
      errors: []
      forces:
        - selections:
            - name: Squad
              children:
                - name: Trooper
                  number: 3
                  children:
                    - name: Weapon
                      number: 6

  # Try to set Weapon to 3 per model (exceeds max=2). Engine allows it but reports error.
  - action: setSelectionCount
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-weapon.selectionId }}
    count: 3

  # Engine sets weapon to 9 (3 per model × 3) and reports validation error
  - expectedState:
      errors:
        - on: selection se-trooper
          from: se-weapon/con-max-2
      forces:
        - selections:
            - name: Squad
              children:
                - name: Trooper
                  number: 3
                  children:
                    - name: Weapon
                      number: 9
      engines:
        newrecruit:
          errors:
            - on: selection se-weapon
              from: se-weapon/con-max-2
