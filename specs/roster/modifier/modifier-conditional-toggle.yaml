id: modifier-conditional-toggle
category: modifier
description: Modifier condition on same entry evaluates correctly, toggling via select/deselect

setup:
  gameSystem:
    forceEntries:
      - id: fe-1
        name: Detachment
  catalogues:
    - id: cat-1
      selectionEntries:
        - id: se-1
          name: Alpha
          type: unit
          modifiers:
            - type: set
              field: name
              value: Active Alpha
              conditions:
                - type: atLeast
                  value: 1
                  field: selections
                  scope: parent
                  childId: se-1

steps:
  - action: addForce
    id: add-detachment
    forceEntryId: fe-1
  # Select Alpha - modifier condition met (atLeast 1 selection of self)
  - action: selectEntry
    id: select-alpha
    forceId: ${{ steps.add-detachment.forceId }}
    entryId: se-1

  - expectedState:
      forces:
        - selections:
            - name: Active Alpha
  # Deselect Alpha
  - action: deselectSelection
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-alpha.selectionId }}

  - expectedState:
      forces:
        - selectionCount: 0
