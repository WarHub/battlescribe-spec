id: force-nested-child-selections
category: force
description: Selections can be added to a child force independently using forcePath
tags: [childForce]

setup:
  gameSystem:
    id: test-gs
    name: Test GS
    forceEntries:
      - id: fe-army
        name: Army
        forceEntries:
          - id: fe-detach
            name: Detachment
  catalogues:
    - id: cat-1
      name: Cat
      gameSystemId: test-gs
      selectionEntries:
        - id: se-1
          name: Squad
          type: unit

steps:
  - action: addForce
    id: add-army
    forceEntryId: fe-army

  - action: addChildForce
    forceId: ${{ steps.add-army.forceId }}
    forceEntryId: fe-army

  # Select a unit INTO the child force using forcePath
  - action: selectEntry
    forceId: ${{ steps.add-army.forceId }}
    entryId: se-1

  - expectedState:
      forceCount: 1
      forces:
        - name: Army
          selectionCount: 0
          childForceCount: 1
          childForces:
            - name: Detachment
              selectionCount: 1
              selections:
                - name: Squad
