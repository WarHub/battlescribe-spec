id: modifier-entry-page
category: modifier
description: Modifier can set an entry's page field (STRING modifier on BaseBookData.page)
tags:
  - known-limitation-synthetic

setup:
  gameSystem:
    id: test-gs
    name: Test GS
    forceEntries:
      - id: fe-1
        name: Detachment
  catalogue:
    id: cat-1
    name: Cat
    gameSystemId: test-gs
    selectionEntries:
      - id: se-1
        name: Marine
        type: unit
        page: "42"
        modifiers:
          - type: set
            field: page
            value: "99"

steps:
  - action: addForce
    forceEntryIndex: 0
  - action: selectEntry
    forceIndex: 0
    entryIndex: 0
  # The modifier targets the entry's page field.
  # Page is informational metadata (BaseBookData.page).
  # With synthetic data, modifiers targeting page may not be evaluated.
  - expectedState:
      forces:
        - selections:
            - name: Marine
