id: modifier-set-boolean
category: modifier
description: Set modifier changes a boolean field (hidden flag)
tags: [design-difference]

setup:
  gameSystem:
    id: test-gs
    name: Test GS
    forceEntries:
      - id: fe-1
        name: Detachment
  catalogues:
    - id: cat-1
      name: Cat
      gameSystemId: test-gs
      selectionEntries:
        - id: se-1
          name: Secret Agent
          type: unit
          modifiers:
            - type: set
              field: hidden
              value: "true"
              conditions:
                - type: atLeast
                  value: 1
                  field: selections
                  scope: self

steps:
  - action: addForce
    id: add-detachment
    forceEntryId: fe-1

  - action: selectEntry
    forceId: ${{ steps.add-detachment.forceId }}
    entryId: se-1

  - expectedState:
      forces:
        - selectionCount: 1
          selections:
            - name: Secret Agent
      engines:
        newrecruit:
          forces:
            - selections:
                - name: Secret Agent
                  hidden: true
          errors:
            - on: selection se-1
              from: se-1/hidden
