id: cost-child-aggregation
category: cost
description: Child selection costs aggregate into roster total

setup:
  gameSystem:
    id: test-gs
    name: Test GS
    costTypes:
      - id: pts
        name: pts
    forceEntries:
      - id: fe-1
        name: Detachment
  catalogues:
    - id: cat-1
      name: Cat
      gameSystemId: test-gs
      selectionEntries:
        - id: se-parent
          name: Squad
          type: unit
          costs:
            - name: pts
              typeId: pts
              value: 50
          selectionEntries:
            - id: se-child
              name: Special Weapon
              type: upgrade
              costs:
                - name: pts
                  typeId: pts
                  value: 15

steps:
  - action: addForce
    id: add-detachment
    forceEntryId: fe-1

  - action: selectEntry
    id: select-squad
    forceId: ${{ steps.add-detachment.forceId }}
    entryId: se-parent

  - expectedState:
      costs:
        - typeId: pts
          value: 50

  - action: selectChildEntry
    forceId: ${{ steps.add-detachment.forceId }}
    selectionId: ${{ steps.select-squad.selectionId }}
    entryId: se-child

  - expectedState:
      costs:
        - typeId: pts
          value: 65
